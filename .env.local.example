# ============================================================
# EnerVision — environnement LOCAL (développement)
#
# Copier en .env.local (jamais commité) :
#   Copy-Item .env.local.example .env.local
#
# Utilisé automatiquement par : .\docker.ps1 local up
# (équivalent : docker compose --env-file .env.local -f docker-compose.yaml
#  -f docker-compose-data.yaml -f docker-compose-keycloak.yaml
#  -f docker-compose-traefik.yml -f docker-compose.local.yml up -d)
#
# Valeurs factices, cohérentes avec keycloak/realm-export.json — jamais
# les vrais secrets de production.
# ============================================================

APP_ENVIRONMENT=


POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PWD=
POSTGRES_APP_USER=
POSTGRES_APP_PWD=

POSTGRES_USER_MLFLOW=
POSTGRES_PASSWORD_MLFLOW=


CORS_ALLOWED_ORIGINS=


KEYCLOAK_URL=
KEYCLOAK_REALM=
KEYCLOAK_CLIENT_ID=


ETL_INTERVAL_SECONDS=
ETL_LOOKBACK_MINUTES=
ETL_FORWARD_FILL_DEPTH=


MLFLOW_TRACKING_URI=
MLFLOW_EXPERIMENT_NAME=
MLFLOW_MODEL_NAME=
MIN_IMPROVEMENT_VS_BASELINE=
MIN_IMPROVEMENT_VS_CHAMPION=
DRIFT_THRESHOLD=
MIN_NEW_ROWS=
PREDICTION_SCHEDULER_ENABLED=
PREDICTION_FORECAST_INTERVAL_MINUTES=
PREDICTION_FORECAST_HOURS=


CORE_URL=
MOCK_API_URL=
POLL_INTERVAL_SECONDS=


AZURE_STORAGE_ACCOUNT=
AZURE_STORAGE_CONTAINER_NAME=
AZURE_SAS_INGESTION=
AZURE_SAS_ETL=
AZURE_SAS_MLFLOW=
